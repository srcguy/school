#include <iostream>

using namespace std;

int main()
{
    for (int i=1; i<=10; i++) //defines 'i', defines loop (if 'i' is smaller than 10 or equal 10 then...), defines what to do after one loop ('i++' means 'i=i+1')
    {
        cout << i << endl;;
    }
}
